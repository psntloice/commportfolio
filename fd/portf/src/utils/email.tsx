// utils/email.ts

export const sendConfirmationEmail = (email: string, confirmationToken: string): void => {
    // This is a placeholder function for sending confirmation emails.
    console.log(`Sending confirmation email to ${email} with token ${confirmationToken}`);
  };
  